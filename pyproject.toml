[project]
name = "lexicon"
version = "0.1.0"
description = "Formal Languages utilities (backend API + frontend)."
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.124.4",
    "uvicorn[standard]>=0.38.0",
    "pydantic-settings>=2.4.0",
    "plotly>=6.5.0",
]
